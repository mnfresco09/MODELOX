# NOTE: This project targets Python >= 3.11.
# If you are on Python 3.9, upgrade Python or adjust pins accordingly.

# Core (data + backtest)
pandas==1.5.3
numpy==1.24.4
## pandas already pinned above
pyarrow>=21.0
polars>=1.0

# Optimization
optuna>=4.4

# Performance (required)
# Pinned to versions that typically ship wheels for macOS + Python 3.11 to avoid local builds.
numba==0.61.2

# Reporting (required for default ejecutar.py reporters)
rich>=14.0
openpyxl>=3.1
plotly>=6.0

# Fast JSON (optional but used when available)
orjson>=3.10

# Machine Learning (for ML strategies)
lightgbm>=4.0
scikit-learn>=1.0
joblib>=1.0


# git push -f origin main
